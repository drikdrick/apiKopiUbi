const users = require('./users');
const products = require('./products');
const stores = require('./stores');
const orders = require('./orders');
const posts = require('./posts');

module.exports = {users, products, stores, orders, posts};
